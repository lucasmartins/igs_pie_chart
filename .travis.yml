rvm:
  - 1.8.7
  - 1.9.2
  - 1.9.3
  - jruby
  - rbx-18mode

before_script: "sudo ntpdate -ub ntp.ubuntu.com pool.ntp.org; true"
script: "bundle exec rake test"

matrix:
  allow_failures:
    - rvm: jruby
    - rvm: rbx-18mode